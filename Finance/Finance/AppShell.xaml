<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Finance.AppShell"
        xmlns:views="clr-namespace:Finance.Views">

    <TabBar>
        <ShellContent
            Title="Costs"
            ContentTemplate="{DataTemplate views:CostsPage}">
        </ShellContent>
        <ShellContent
            Title="Auth"
            ContentTemplate="{DataTemplate views:AuthPage}">
        </ShellContent>

    </TabBar>
</Shell>